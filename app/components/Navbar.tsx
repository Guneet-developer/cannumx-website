"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";
const links = [{ name: "Capabilities", href: "#services" }, { name: "How we work", href: "#how-it-works" }, { name: "Industries", href: "#industries" }];
export default function Navbar() { const [open, setOpen] = useState(false); return <nav className="fixed inset-x-0 top-0 z-50 border-b border-white/10 bg-[#102019]/80 text-[#f3f2ec] backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 sm:px-8 lg:px-12"><Link href="/#home" className="text-xl font-semibold tracking-[-.07em]">cannum<span className="text-[#caff5a]">x</span></Link><div className="hidden items-center gap-7 md:flex">{links.map(link => <Link key={link.name} href={link.href} className="text-sm text-white/65 transition hover:text-[#caff5a]">{link.name}</Link>)}<Link href="/contact" className="rounded-full border border-[#caff5a] px-4 py-2 text-sm text-[#caff5a] transition hover:bg-[#caff5a] hover:text-[#102019]">Let&apos;s talk</Link></div><button className="md:hidden" aria-label="Toggle menu" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div>{open && <div className="border-t border-white/10 px-5 py-5 md:hidden">{links.map(link => <Link onClick={() => setOpen(false)} key={link.name} href={link.href} className="block py-2 text-white/70">{link.name}</Link>)}<Link onClick={() => setOpen(false)} href="/contact" className="mt-2 block py-2 text-[#caff5a]">Let&apos;s talk →</Link></div>}</nav>; }
